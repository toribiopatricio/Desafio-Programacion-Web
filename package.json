{
  "name": "package.json",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass scss/estilos.scss css/estilos.css",
    "nodemon": "nodemon -e sass -x \"npm run build-css\"",
    "watch-sass": "sass --watch scss/estilos.scss css/estilos.css"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "node-sass": "^5.0.0",
    "nodemon": "^2.0.6"
  },
  "dependencies": {
    "sass": "^1.32.0"
  }
}
